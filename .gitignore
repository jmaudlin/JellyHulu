node_modules/
.DS_Store
*.log
.cache/
tmp/

# .NET build output
[Bb]in/
[Oo]bj/
plugin/artifacts/
*.user

# Test output. Browser rendering is not byte-deterministic, so these differ
# between runs; keeping them out of the tree stops every test run dirtying it.
# The screenshot the README embeds lives in docs/ and is updated on purpose.
test/output/
